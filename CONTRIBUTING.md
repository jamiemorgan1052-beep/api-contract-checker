# Contributing

Issues and focused pull requests are welcome. Please:

1. Preserve deterministic, dependency-free validation.
2. Use synthetic JSON fixtures only.
3. Include tests for new contract rules and error paths.
4. Run `python -m unittest discover -s tests -v` before submitting.

Keep error locations stable and machine-readable where possible.
